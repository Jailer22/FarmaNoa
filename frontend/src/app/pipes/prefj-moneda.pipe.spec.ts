import { PrefjMonedaPipe } from './prefj-moneda.pipe';

describe('PrefjMonedaPipe', () => {
  it('create an instance', () => {
    const pipe = new PrefjMonedaPipe();
    expect(pipe).toBeTruthy();
  });
});
